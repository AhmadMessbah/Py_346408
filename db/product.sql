create table if not exists products(
    id integer primary key autoincrement,
    name text,
    brand text,
    modedl text,
    serial text,
    catagory text,
    unit text,
    expiration_date text
);